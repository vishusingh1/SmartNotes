import { Hono } from "hono";
import { Setup } from "./index";
export type Server = {
    start: (params: {
        host: string;
        port: number;
        cb: (err: Error | null, address: string) => void;
    }) => void;
};
export declare const prepareServer: (setup: Setup, app?: Hono) => Promise<Server>;
